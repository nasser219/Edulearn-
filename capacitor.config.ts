import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.tarbaweyeen.app',
  appName: 'npm run buildnpx cap add androidnpx cap copynpx cap open android',
  webDir: 'dist'
};

export default config;
